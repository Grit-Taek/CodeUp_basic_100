#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    int a;
    scanf("%d",&a);
    int arr[a];
    int d =0;
    for(int i=0;i<a;i++)
    {
        scanf("%d",&d);
        arr[i] = d;
    }

    sort(arr, arr+a);

    printf("%d",arr[0]);


    return 0;
}
